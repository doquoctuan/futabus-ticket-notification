apiVersion: v1
kind: Secret
metadata:
  name: futabus-backend-secret
  namespace: futabus
  labels:
    app: futabus-backend
type: Opaque
stringData:
  # Auth0 Configuration
  AUTH0_DOMAIN: "dev-zn1uoyrjdwgocc2l.us.auth0.com"
  AUTH0_AUDIENCE: "https://api-futabus"
  
  # Database Configuration
  DATABASE_HOST: "tuando-17197.j77.aws-ap-southeast-1.cockroachlabs.cloud"
  DATABASE_USERNAME: "app"
  DATABASE_PASSWORD: "0Pqmn0_tzeHZEISVGz5RKQ"
  
  # Note: In production, use Sealed Secrets or external secret management
  # Example: kubectl create secret generic futabus-backend-secret \
  #   --from-literal=AUTH0_DOMAIN=your-tenant.auth0.com \
  #   --from-literal=DATABASE_PASSWORD=secure-password \
  #   -n futabus
